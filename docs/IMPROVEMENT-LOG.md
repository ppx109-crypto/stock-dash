# 오늘의 투자판단 · 개선 기록

30분마다 한 회차씩, 점수가 가장 낮은 항목을 고치고 다시 점수를 매깁니다.
2026-09-20 07:03 UTC에 시작해 12시간(24회차, 19:03 UTC까지) 돌립니다.
회차마다 타이머를 다시 겁니다.

## 목표

**'오늘의 투자판단'을 누르면, 그룹별로 나뉜 '무엇을 투자해야 하는가'가 한눈에 보인다.**

## 방향성 · 이 사이트가 지키는 것

교재(START-HERE.md·README.md)에서 읽은 원칙입니다. 새 기능은 이 선을 넘지 않습니다.

- 공식 자료만 씁니다. 공공데이터포털 시세, DART 결산·공시.
- 모든 숫자에 기준일과 출처를 답니다. 종가를 실시간 체결가라 부르지 않습니다.
- 같은 기간·회계기준끼리 비교합니다. 누적과 분기를 섞지 않습니다.
- 추정은 추정이라 적습니다. 참고가는 매수·매도 신호가 아닙니다.
- 자료가 없으면 없다고 적습니다. 빈자리를 그럴듯한 숫자로 채우지 않습니다.
- 개인 보유수량·계좌·비밀번호는 공개 자료에 넣지 않습니다.

## 판단 규칙 (현재)

일봉 종가가 EMA 5·20·40·60 각각의 위에 있는지 네 가지를 셉니다.
넷 다 충족이면 A(투자적기), 20·40·60만 충족이면 B(투자보류), 그 밖은 C(아직보류).
영업이익은 그룹에 넣지 않고 종목 화면의 근거로 씁니다.

## 점수표

1(전혀 안 됨) ~ 5(목표 달성). 회차마다 다시 매깁니다.

| # | 항목 | 1회차 |
|---|---|---|
| # | 항목 | 1회차 전 | 1회차 후 |
|---|---|---|---|
| 1 | 첫 화면이 클릭 없이 그룹별 답을 보여준다 | 2 | 4 |
| 2 | 그룹 안 순서가 '무엇을 먼저 볼까'에 답한다 | 2 | 4 |
| 3 | 종목을 고르면 판단 근거가 충분하다 | 4 | 4 |
| 4 | 자료 없는 종목이 자료 있는 종목처럼 보이지 않는다 | 3 | 3 |
| 5 | 모든 숫자에 기준일·출처가 붙는다 | 3 | 3 |
| 6 | 좁은 창에서도 읽힌다 | 3 | 4 |
| 7 | 판단 규칙이 화면에서 설명된다 | 2 | 4 |
| 8 | 자료 없는 종목을 화면에서 채울 수 있다 | 2 | 4 |
| 9 | 화면이 빠르다 (호출·캐시) | 4 | 4 |
| 10 | 자료가 없거나 규칙이 바뀌어도 화면이 멈추지 않는다 | 4 | 4 |

## 회차 기록

### 1회차 · 2026-09-20

- 고른 항목: 1번(첫 화면), 2번(정렬 기준) — 둘 다 2점으로 가장 낮음
- 사용자 확인: 첫 화면은 "그룹 + 종목 다 보이게", 정렬은 "EMA 충족 + 실적"
- 한 일
  - 접힌 미니 보드를 없애고 A·B·C 그룹판을 펼친 채로 첫 화면에 둡니다. 각 칸에
    종목 이름과 `EMA 4/4 · 실적 양호`가 함께 보입니다.
  - 같은 그룹 안 순서를 EMA 충족 수 → 영업이익 등급 → 영업이익 충족 수 → 이름
    순으로 정했습니다(`rank_rows`). 그룹판과 종목 버튼이 같은 순서를 씁니다.
  - 판단 규칙 한 줄(`RULE_TEXT`)을 그룹판 제목 옆에 적었습니다.
  - 자료 없는 종목을 화면에서 채우는 버튼을 넣었습니다. DART 결산·공시를 종목마다
    받아 저장하고, 한 번에 20종목씩, 받은 종목은 그때그때 저장합니다.
  - `cached_report`가 원본 저장소만 보던 것을 이 저장소도 보도록 고쳤습니다.
- 렌더링에서 잡은 결함 2건
  - 그룹 칸의 설명 자리에 칩 문구가 찍혔습니다. 반복문 변수 이름이 바깥 `note`를
    가린 탓이라 `chip_note`로 바꿨습니다.
  - 좁은 창에서 규칙 한 줄이 화면 밖으로 나갔습니다. 720px 아래에서 줄바꿈하도록
    고쳤습니다.
- 다음 회차 후보: 4번(자료 없는 종목 구분), 5번(기준일·출처 표기)
